import React from 'react';

const Course = (props) => (
    <div className="main-content">
        <div className="container">
            <h2>{props.title}</h2>
        </div>
    </div>
)

export default Course;